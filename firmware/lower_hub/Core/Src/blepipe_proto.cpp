/* The BLEPIPE wire codec is shared with U9. Keep the LowerHub translation
 * unit in the project so the linker receives the C-linkage definitions. */
#include "../../../master/Core/Src/ble/blepipe_proto.cpp"
